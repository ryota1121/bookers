<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th></th>
    </tr>
  </thead>
        
  <tbody>
  <% @books.each do |book| %>
    <tr>
      <td><%=　link_to book.title,book_path(book.id)  %></td>
      <td><%= link_to book.body,book_path(book.id)  %></td>
    </tr>
  <% end %>
  </tbody>

 </table>